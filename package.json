{
  "name": "url-blocker-extension",
  "version": "1.0.0",
  "description": "A Chrome extension to block unwanted URLs and manage web browsing with customizable filtering rules",
  "scripts": {
    "lint": "eslint src/ *.js",
    "lint:fix": "eslint src/ *.js --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "validate": "npm run lint && npm run format:check",
    "package": "npm run validate && zip -r url-blocker-extension.zip . -x node_modules/\\* .git/\\* *.zip"
  },
  "devDependencies": {
    "eslint": "^8.54.0",
    "prettier": "^3.1.0"
  },
  "keywords": [
    "chrome-extension",
    "url-blocker",
    "web-filter",
    "productivity"
  ],
  "author": "",
  "license": "MIT"
}
